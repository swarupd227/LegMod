spring:
  application:
    name: cap-service-test
  data:
    jdbc:
      dialect: postgresql
  flyway:
    enabled: true
    schemas: cap
    default-schema: cap
    create-schemas: true
    baseline-on-migrate: true
    baseline-version: 0
    locations: classpath:db/migration

PROV_ENABLED: false
PROV_SERVICE_URL: http://localhost:1
