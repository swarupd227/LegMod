spring:
  application:
    name: arch-service-test
  data:
    jdbc:
      dialect: postgresql
  flyway:
    enabled: true
    schemas: arch
    default-schema: arch
    create-schemas: true
    baseline-on-migrate: true
    baseline-version: 0
    locations: classpath:db/migration

PROV_ENABLED: false
PROV_SERVICE_URL: http://localhost:1
LLM_GATEWAY_URL:  http://localhost:1
